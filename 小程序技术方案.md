# 小程序版技术方案（uni-app 架构 + 类目包装）

> 前提：网页版上线计划已完成（认证/数据库/沙箱就绪），后端 API 已带 token 鉴权。
> 总工时：约 22~32 个工作日（1 名全栈），含提审约 5~7 周。
> 核心策略：**小程序做获客与轻场景，重度环节（手撕代码）留在 H5/PC**。

---

## 1. 产品定位与功能裁剪

| 功能 | 小程序端 | 说明 |
|---|---|---|
| 注册登录 | ✅ 微信静默登录 | 体验反而优于网页版 |
| 八股问答模式（quiz） | ✅ 完整支持 | 纯文本对话，最适合手机 |
| 项目深挖 / 自我介绍 | ✅ 完整支持 | 文本 + 语音均可 |
| **手撕代码环节** | ⚠️ 降级 | 纯文本框 + 等宽字体，无语法高亮；界面提示「代码题建议在电脑完成，扫码打开网页版」 |
| 语音模式 | ✅ 接同声传译插件 | 识别质量略低于 Chrome，中英混说术语场景需提示用户 |
| 简历/JD 上传 | ✅ `wx.chooseMessageFile` | 从微信聊天选文件，契合「HR 发来 JD」场景 |
| 历史 / 错题本 / 报告 | ✅ 完整支持 | 报告卡片化，**加「分享到群」按钮（核心裂变点）** |
| Pro 付费 | 二期，需企业主体 + 虚拟支付 | 一期免费 + 场次限制 |
| 面试提醒 | ✅ 订阅消息 | 网页端做不到的触达能力 |

---

## 2. 技术选型

**uni-app（Vue3 + TypeScript）+ pinia + uview-plus（或 wot-design-uni）组件库**

理由：
1. 一套代码同时编译到微信小程序 + H5 + APP——H5 版可直接替换现在的原生 JS 前端，APP 壳将来顺手产出
2. Vue3 生态对团队后续招前端友好；当前 `app.js` 1400 行手写 DOM 操作正好借重构清理
3. 对比 Taro（React）：两者都可行，uni-app 对小程序 API 兼容更直接、中文资料更多

不选原生小程序开发：多端输出是明确需求（用户群在手机上也要 H5 兜底代码环节）。

---

## 3. 工程结构

```
miniprogram-app/
├── src/
│   ├── pages/
│   │   ├── index/           # 启动面板（选模式/方向/难度/人设）
│   │   ├── interview/       # 面试主界面（对话流 + 阶段指示器）
│   │   ├── coding/          # 代码答题页（降级文本框版）
│   │   ├── report/          # 报告页（雷达图 + 分享）
│   │   ├── history/         # 历史列表
│   │   ├── mistakes/        # 错题本（code/quiz 双 tab）
│   │   └── profile/         # 我的（登录态/版本/订阅消息开关）
│   ├── components/
│   │   ├── ChatBubble.vue       # 消息气泡（Markdown 渲染）
│   │   ├── StreamRenderer.vue   # 流式打字机渲染
│   │   ├── StageIndicator.vue   # 四环节阶段指示
│   │   ├── VoicePanel.vue       # 语音输入/朗读控制
│   │   └── ReportCard.vue       # 报告分享卡片
│   ├── api/
│   │   ├── request.ts       # uni.request 封装 + token 注入 + 401 处理
│   │   └── stream.ts        # enableChunked 流式封装（见 §4.1）
│   ├── stores/              # pinia: user / session / interview
│   ├── utils/
│   └── static/vendor/       # marked、dompurify 本地化（小程序不允许远程 JS）
├── manifest.json
└── pages.json
```

雷达图：用 `echarts-for-weixin`（ec-canvas）或 uCharts，小程序内可用。

---

## 4. 关键技术方案

### 4.1 流式输出（最重要）

- 微信小程序：`uni.request({ enableChunked: true })` + `onChunkReceived`，把后端 SSE 字节流按 `data:` 行切分解析。要求基础库 ≥ 2.20.1（2021 年，覆盖率 99%+，低版本直接提示升级微信）。
- H5 端：沿用现有 `fetch + ReadableStream` 方案。
- 封装统一接口 `streamChat(params, { onToken, onStage, onProblem, onDone, onError })`，两端实现差异收敛在 `api/stream.ts` 内用条件编译（`#ifdef MP-WEIXIN`）处理。
- **降级**：个别老安卓 chunked 异常时，退化为「完成后一次性返回」（后端 SSE 本来就有 done 事件携带全文）。
- 后端 SSE 格式**无需改动**（token/stage/problem/report_start/done 事件原样复用）。

### 4.2 登录

- `wx.login()` 拿 code → `POST /api/wx_login {code}` → 后端调 `code2session` 换 openid → 查 users 表（Phase 1 已预留 openid 字段）→ 无则自动建号 → 返回 JWT。
- 之后所有请求走 `Authorization: Bearer`，与网页版同一套鉴权。
- 用户在网页版已注册？「我的」页提供绑定：小程序内扫码或输入账号密码合并 openid（二期）。

### 4.3 语音模式

- 插件：微信官方「同声传译」插件（小程序后台申请，免费），提供 ASR（`start`/`stop` 流式识别）+ TTS。
- 对应替换 `voice.js` 中的 `SpeechRecognition` / `speechSynthesis`，接口层抽象一致：面试官朗读（TTS）、开麦回答（ASR）、静音 2s 自动发送。
- 已知短板：中英混说术语识别率低于 Chrome。UI 上加「识别不准可点文字编辑后再发送」。
- 网络型 ASR（腾讯云/讯飞）一期不接，成本与复杂度不值。

### 4.4 文件上传

- `wx.chooseMessageFile({type:'file', extension:['pdf','docx','txt']})` → `uni.uploadFile` 到现有 `/api/upload_resume`（加 token 头）。
- 后端零改动（5MB 上限、魔数校验原样生效）。

### 4.5 代码编辑降级

- 等宽字体 textarea + 常用符号快捷栏（`{ } ( ) [ ] ; = < > : ,`）+ tab 插入 4 空格。
- 提交后走同一 `/api/submit_code`（SSE 判题 + 点评），判题结果展示不变。
- 页面顶部常驻提示：「在电脑上获得完整代码编辑体验 → 网页版网址 / 扫码」。

### 4.6 分享与订阅消息（获客主引擎）

- 报告页 `onShareAppMessage`：卡片标题「我在这场 AI 模拟练习拿了 82 分，你敢来试试吗」+ 雷达图缩略。
- 面试开始前申请订阅消息授权：开始前 15 分钟提醒、错题 3 日未复习提醒。
- 历史页「邀请好友对战/对比分数」二期做。

---

## 5. 后端新增/改造点（约 5~7 天，含在上面总工时内）

| 改造 | 内容 | 工时 |
|---|---|---|
| `/api/wx_login` | code2session 换 openid，建号/绑号，发 JWT | 1 天 |
| 内容安全 | 用户输入（简历文本、聊天）过 `security.msgSecCheck`，简历文件过 `security.mediaCheckAsync`；不合规返回固定话术 | 1~1.5 天 |
| 订阅消息 | 模板申请 + `/api/notify/subscribe` 记录 + 定时发送任务 | 1~1.5 天 |
| 分享统计 | 报告卡片带 `from_uid`，新用户经分享进入记录来源（北极星指标需要） | 0.5 天 |
| 限流 | 每用户每日面试场次上限（防 LLM 成本失控，免费期尤其重要） | 0.5 天 |

---

## 6. 类目包装与文案（过审关键）

### 6.1 类目选择

- **首选：「工具 > 效率」**（个人主体可选，无需资质）
- 备选：「工具 > 办公」
- **绝不选**：教育、人力资源——个人主体不可选，企业主体也需许可证（ICP 许可证 / 人力资源服务许可证，均为月级办理周期）

### 6.2 措辞规范（红线词 → 安全词）

| 避免出现 | 替换为 |
|---|---|
| 面试、模拟面试 | 对话练习、模拟对话、表达练习 |
| 求职、应聘、找工作 | 自我提升、能力训练 |
| 招聘、HR、猎头 | （不出现） |
| offer、保过、上岸 | 练习目标、进步曲线 |
| 培训、课程、教学、辅导 | 练习、自测、评测 |
| 简历（名「简历优化」类功能描述慎用） | 个人介绍材料、背景材料 |
| 面试官 | 练习官 / AI 对话伙伴 |

> 注意：功能本身不变（产品内该怎么做还怎么做），以上措辞只用于**对外可见文本**——小程序名称、简介、页面标题、菜单、分享卡片。审核只看得到你呈现的文字。

### 6.3 命名与简介建议

- 名称候选：「言练 · AI 表达练习」「对话练习官」「言值练习室」（避免「面试」二字；上线稳定后可改名，改名有次数限制需谨慎）
- 简介：「AI 驱动的表达与思维练习工具。模拟真实对话场景，覆盖技术问答、项目复盘、临场反应训练，生成多维度能力报告，帮你持续提升表达力。」
- 页面文案：启动面板写「选择练习方向」，不写「选择面试岗位」；报告页写「能力报告」，不写「面试结果」；verdict「通过/待定/不通过」改为「优秀/达标/待提升」。

### 6.4 审核避坑清单

1. 提审前自查：全小程序搜索「面试、求职、招聘、offer、培训」字样，一个不留（含图片内文字）
2. 演示账号：审核版提供体验入口（审核员要能用），但演示数据里同样不能有红线词
3. UGC 安全：必须已接入 `msgSecCheck`（见 §5），否则 AI 生成内容类容易被驳回
4. 用户隐私：隐私协议勾选（收集微信昵称/openid、上传文件用途说明），小程序后台「用户隐私保护指引」如实填写
5. 若被驳回：按驳回理由改措辞重提即可，不是处罚；预留 2~3 轮提审的心理预期（每轮 1~7 天）

---

## 7. 排期汇总

| 周 | 内容 |
|---|---|
| W1 | uni-app 脚手架 + 登录（wx_login）+ API 层 + 启动面板/我的页面 |
| W2 | 面试主界面：对话流（enableChunked）+ 阶段指示 + Markdown 渲染 |
| W3 | quiz/项目模式联调 + 报告页（雷达图 + 分享卡片） |
| W4 | 代码页降级版 + 历史 + 错题本 |
| W5 | 语音（同声传译插件）+ 上传 + 订阅消息 + 内容安全接入 |
| W6 | 文案合规自查 + 体验账号 + 提审（预留 1~2 轮驳回修改） |

> 若与网页版并行推进（网页版 3 周先行），小程序启动时后端已就绪，总周期「网页上线后 +6 周」。
