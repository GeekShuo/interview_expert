一、先说结论（按 1 名全栈开发全职估算）
方案	开发工时	流程等待	上线总周期	首年硬成本	适合阶段
在线网页部署	2~3 周	备案 1~3 周（可并行）	3~5 周	约 1000~2000 元/年	现在就该做
微信小程序	4~6 周（不含支付）	备案+审核 1~2 周	6~8 周	约 500~2000 元/年 + 服务器	商业化第一步
APP（套壳）	2~3 周	上架+备案 2~4 周	4~6 周	约 1500~3000 元/年	不推荐现在做
APP（Flutter 原生）	8~10 周	同上	10~12 周	同上	有稳定收入后再说
若业余时间开发（每周 1520 小时），工时 ×2.53。

二、无论选哪条路，都必须先做的「上线前置改造」
当前代码是单机演示级，直接公网部署有三个硬伤，这部分是各方案的共同成本（约 8~12 人天）：

改造项	现状	工作量	风险
认证体系重做	明文密码 + 只靠前端的 user_id 参数隔离，后端零鉴权——任何人传别人 ID 就能看其历史和错题	3~5 天	不改上线就是安全事故
JSON 文件 → 真实数据库	data/*.json + 文件锁	2~4 天（SQLite 即可，量大了再迁 PG）	并发一高就丢数据
代码沙箱隔离	runner.py 直接 subprocess 跑用户代码，注释里自己也写了"上线前需换 Docker/Judge0"	3~5 天	用户代码可读写服务器文件，最高危项
三、方案一：在线网页部署（推荐第一步）
工作量（2~3 周）
前置改造（见上表）：8~12 天
Nginx 反代 + HTTPS + uvicorn 守护 + 日志：1~2 天
CDN 资源本地化（Tailwind/Monaco/marked 目前全走 jsdelivr CDN，国内不稳）：1 天
Docker 化（可选）：1~2 天
配置清单
项	建议	费用
服务器	腾讯云/阿里云轻量 2C4G（2C2G 够跑，留余量给沙箱）	60~150 元/月
域名	.com / .cn	30~70 元/年
ICP 备案	免费，流程 1~3 周，期间无法对外访问	0
HTTPS	云厂商免费证书 / Let's Encrypt	0
DeepSeek API	一场完整面试约 5~15 万 tokens	约 0.1~0.3 元/场
月成本 ≈ 100~200 元（100 个活跃用户以内）。主要变动成本是 LLM 调用，极便宜。

移动端体验
前端已有基础响应式（md: 断点 + 触屏检测），但 Monaco 编辑器在手机上体验差（TODO 里也标了这个风险），手机上写代码环节建议降级为纯文本框。可顺手加 PWA（manifest + Service Worker，1~2 天），让手机"添加到主屏幕"，零成本获得类 APP 入口。

四、方案二：微信小程序（商业化推荐路径）
必须改造的点（这是工时的主要来源）
障碍	现状	小程序方案	工作量
前端整体重写	原生 JS 2100 行，无框架	用 uni-app 重写（一套代码同时出小程序 + APP + H5，为以后铺路）	12~18 天
流式输出	fetch + ReadableStream 读 SSE	小程序 wx.request 开 enableChunked 可接流式，但需重写解析层	含在上面
Monaco Editor	小程序不可用	降级为代码文本框（手机上手撕代码本来就不现实）；或面试核心环节保留 web-view 嵌 H5	1~2 天
语音模式	浏览器 Web Speech API，小程序完全不支持	接微信同声传译插件（官方免费插件，ASR+TTS 都有）；要求更高则用腾讯云 ASR/TTS（约 2~4 元/小时）	2~3 天
简历上传	<input type=file>	改 wx.chooseMessageFile（从微信聊天选文件），天然契合"别人发我的 JD/简历"场景	0.5 天
登录	用户名密码	wx.login 换 openid，静默登录，体验反而更好	2~3 天
Pro 付费	目前是占位弹层	微信支付 + 虚拟商品合规	3~5 天
资质与费用（容易踩的坑）
个人主体 vs 企业主体：个人小程序免费注册，但不能开通微信支付、且"教育/招聘"类目受限。要收 Pro 费用必须注册个体工商户或公司（注册成本约 0~2000 元）+ 微信认证 300 元/年 + 微信支付费率 0.6%。
小程序备案：2023 年起强制，免费，约 1~2 周。
内容安全：用户上传简历 + LLM 生成内容属 UGC，需接微信内容安全检测 API（免费），否则可能被下架。
后端域名：必须 HTTPS + 已 ICP 备案，配置到 request 合法域名。
小结
开发 46 周 + 流程 12 周，1 人完成。 外包市场价约 3~8 万。硬成本主要是服务器（复用网页版的）+ 300 元/年认证费。

五、方案三：APP
三条子路线
子路线	做法	工时	评价
套壳	uni-app 的 APP 端直接打包（如果方案二用了 uni-app，这步几乎白送）	1~2 周	性价比最高，体验够用
Capacitor 套壳	把现有 Web 前端直接包成 APP	1~2 周	但 Web Speech API 在 WebView 里不稳定，Monaco 触屏难用
Flutter 原生重写	双端一套代码	8~10 周	现阶段过度投资
上架成本（比小程序麻烦得多）
APP 备案（工信部，2023 年强制）：免费，1~3 周
软著：上架国内安卓商店必备，普通流程 300800 元 / 13 个月，加急更贵
苹果开发者：$99/年（约 700 元）；iOS 内购虚拟商品苹果抽成 30%（小程序走微信支付则避开）
国内安卓商店（华为/小米/OPPO/vivo）：分别注册开发者、分别提审，部分要求企业资质，首次上架全程 2~4 周
结论
现在不建议做 APP。 理由：① 面试是低频重场景，用户不愿为此装 APP；② 上架流程成本远超小程序；③ 若前端用 uni-app 重构，APP 端是顺手产物，届时再上架不迟。

六、推荐路线图
code
第 1~3 周    网页版上线（认证+数据库+沙箱加固+服务器+备案启动）
第 4~5 周    真实用户验证 Pro 付费意愿（你们 chanpin 文件正是审这个的）
第 5~10 周   uni-app 重构 → 微信小程序（含微信支付）→ 顺带产出 APP 壳
第 10 周后   根据数据决定是否上架应用商店
总预算（1 人全职 2.53 个月，或外包 510 万）：

首年硬成本：约 2500~5000 元（服务器 1500 + 域名/认证/备案 500 + 苹果账号可选 700 + 软著可选 500）
单场面试 LLM 成本约 0.10.3 元，Pro 定价 9.919.9 元/月即可有 95%+ 毛利
一个关键决策前置：先注册个体工商户/公司（几百元、1 周左右），否则微信支付、小程序类目、安卓商店全部卡壳——这个可以这周就启动，和开发完全并行。


