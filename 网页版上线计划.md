# 网页版上线计划（认证 + SQLite + 沙箱加固 + 部署）

> 目标：把当前单机 MVP 改造成可安全公网运行的多用户服务。
> 总工时：约 13~18 个工作日（1 名全栈），含联调测试约 3 周。
> 原则：每一步做完系统仍可运行（可随时回滚），先安全后体验，不重写业务逻辑。

---

## 总体排期

| 阶段 | 内容 | 工时 | 依赖 |
|---|---|---|---|
| Phase 0 | 数据备份 + 配置规范化 | 0.5 天 | 无 |
| Phase 1 | 认证体系重做 | 3~4 天 | Phase 0 |
| Phase 2 | JSON 文件 → SQLite | 3~4 天 | Phase 1（users 表先行） |
| Phase 3 | 代码沙箱隔离 | 3~4 天 | 可与 Phase 2 并行（需先装 Docker） |
| Phase 4 | 部署上线（Docker/Nginx/HTTPS/CDN 本地化） | 2~3 天 | Phase 1~3 完成 |
| 缓冲 | 联调、压测、修 bug | 2 天 | — |

> ICP 备案（1~3 周）在 Phase 1 启动当天就提交，与开发完全并行，不占开发工时。

---

## Phase 0：准备（0.5 天）

- [ ] 备份 `backend/data/` 全量（33 个真实会话快照 + accounts/history/mistakes）
- [ ] `.env` 收敛：`LLM_API_KEY`、`LLM_MODEL`、新增 `JWT_SECRET`、`DB_PATH`、`SANDBOX_MODE`；`.env.example` 同步更新
- [ ] `pyproject.toml` 增加依赖：`passlib[bcrypt]`、`pyjwt`（或 `itsdangerous`）、`aiosqlite`（可选，先用标准库 `sqlite3` + 线程锁即可）
- [ ] 修正端口不一致：README/cpolar 说 8000，`run_server.sh` 默认 8001，统一为 8000

**验收**：`uvicorn app.main:app` 正常启动，现有功能回归通过。

---

## Phase 1：认证体系重做（3~4 天）

现状：`accounts.json` 明文密码；后端零鉴权，前端凭 URL 里的 `user_id` 隔离数据——任何人传别人的 ID 即可看其历史/错题。这是上线前**最高优先级**。

### 任务分解

1. **`accounts.py` 重写**（1 天）
   - users 存储改为 SQLite 表（Phase 2 的 `db.py` 先行建 users 表，避免二次迁移）：
     ```sql
     CREATE TABLE users (
       id INTEGER PRIMARY KEY AUTOINCREMENT,
       username TEXT UNIQUE NOT NULL,
       nickname TEXT NOT NULL,
       password_hash TEXT NOT NULL,
       tier TEXT NOT NULL DEFAULT 'free',      -- free / pro
       openid TEXT UNIQUE,                      -- 预留：小程序阶段绑定微信
       created_at TEXT NOT NULL
     );
     ```
   - 密码用 bcrypt 哈希（passlib），注册/登录/改密接口
   - 迁移脚本：`accounts.json` → users 表（预置 alice/bob/carol/dave 密码重置为随机值，首次登录强制改密，或直接废弃演示账户）

2. **Token 机制**（1 天）
   - 新增 `app/auth.py`：JWT 签发/校验（HS256，7 天过期，`JWT_SECRET` 从 .env 读）
   - `POST /api/login` 返回 `{token, user}`；新增 `POST /api/register`、`GET /api/me`
   - FastAPI 依赖注入 `get_current_user(request) -> user_id`，从 `Authorization: Bearer <token>` 解析

3. **全端点鉴权改造**（0.5~1 天）
   - `main.py` 16 个端点全部接入 `get_current_user`，**移除所有来自 query/body 的 user_id 信任**
   - `/api/history`、`/api/mistakes`、`/api/state`、`/api/chat` 等一律以 token 中的 user_id 为准
   - 游客模式保留：未登录时发匿名 token（前端 `ie_uid` 迁入 token 的 sub 字段），数据隔离规则不变

4. **前端适配**（0.5 天）
   - `app.js`：登录后存 token（`ie_token`），所有 fetch 加 `Authorization` 头；401 时跳登录弹层
   - 注册界面（用户名+密码+昵称）

**验收**：① 不带 token 请求受保护接口返回 401；② 用 A 的 token 请求 B 的 history 返回空/403；③ 数据库中无明文密码；④ 旧数据（history.json 里的 user_id=alice 等）迁移后归属正确。

---

## Phase 2：JSON 文件 → SQLite（3~4 天）

现状：`data/history.json`（上限 200 条）、`mistakes.json`、`live_sessions/*.json` + 跨进程文件锁。并发下既慢又有丢数据风险。

### 任务分解

1. **新增 `app/db.py`**（0.5 天）
   - `sqlite3` 连接管理（`check_same_thread=False` + `threading.Lock`，或 WAL 模式）
   - 建表：
     ```sql
     CREATE TABLE interviews (          -- 替代 history.json
       id TEXT PRIMARY KEY,             -- 沿用现有会话 id
       user_id TEXT NOT NULL,
       started_at TEXT, finished_at TEXT,
       persona_name TEXT, persona_title TEXT, jd_title TEXT,
       score INTEGER, verdict TEXT, abandoned INTEGER,
       dimensions TEXT,                 -- JSON 字符串
       judge_passed INTEGER, judge_total INTEGER,
       weak_tags TEXT, report TEXT, transcript TEXT, tier TEXT
     );
     CREATE INDEX idx_interviews_user ON interviews(user_id, started_at DESC);

     CREATE TABLE mistakes (            -- 替代 mistakes.json，code/quiz 用 type 区分
       id INTEGER PRIMARY KEY AUTOINCREMENT,
       user_id TEXT NOT NULL,
       type TEXT NOT NULL,              -- code / quiz
       problem_id TEXT, title TEXT, difficulty TEXT, tags TEXT,
       question TEXT, direction TEXT,
       wrong_times INTEGER DEFAULT 1,
       last_passed INTEGER, last_total INTEGER,
       updated_at TEXT
     );
     CREATE INDEX idx_mistakes_user ON mistakes(user_id, type);

     CREATE TABLE live_sessions (       -- 替代 live_sessions/*.json
       session_id TEXT PRIMARY KEY,
       user_id TEXT NOT NULL,
       snapshot TEXT NOT NULL,          -- 整份快照 JSON 原样存
       updated_at TEXT
     );
     ```
   - `live_sessions` 直接存 JSON 快照字符串（28 个字段的快照结构不变），改造成本最低

2. **存储层改造**（1.5~2 天）
   - `history.py`：读写改 SQL，保留"每用户上限 200 条"清理逻辑（改为 `DELETE ... WHERE id NOT IN (SELECT ... LIMIT 200)`）
   - `mistakes.py`：读写改 SQL
   - `session.py`：快照落盘点（原子写 + 文件锁）改为 `INSERT OR REPLACE INTO live_sessions`
   - 删除文件锁代码（fcntl/msvcrt 分支）

3. **迁移脚本**（0.5 天）
   - `scripts/migrate_json_to_sqlite.py`：读 `data/*.json` + `live_sessions/*.json` → 入库
   - 注意 user_id 映射：JSON 里的 `user_id`（alice/匿名 ie_uid）与 Phase 1 的 users 表对齐——匿名用户数据沿用原 `ie_uid` 字符串即可

4. **备份机制**（0.5 天）
   - 每日 cron：`sqlite3 data/app.db ".backup data/backups/app-$(date +%F).db"`，保留 14 天

**验收**：① 迁移前后历史/错题条数一致、抽查内容一致；② 服务重启后进行中的面试可从 `live_sessions` 恢复；③ 并发 10 请求写历史无丢失；④ 200 条上限清理仍生效。

---

## Phase 3：代码沙箱加固（3~4 天）

现状：`runner.py` 直接 `subprocess + python -I` 跑用户代码，仅 8s 超时 + 输出截断——用户可以 `import os; open('/etc/passwd')`、读 `.env` 里的 LLM 密钥、死循环吃 CPU。**这是上线最大的安全洞。**

### 方案选择

| 方案 | 隔离强度 | 工作量 | 结论 |
|---|---|---|---|
| A. Docker 容器判题（每题起容器） | 高 | 2~3 天 | **推荐**：自建、零外部依赖 |
| B. Judge0（开源判题服务，docker-compose 部署） | 高 | 2~3 天 | 功能全但重，判题类型只有 func/链表/树/ops 四种，用不上其复杂度 |
| C. 继续 subprocess + 资源限制（rlimit + 只读挂载） | 中 | 1 天 | 兜底方案，仍有逃逸面，不推荐正式用 |

### 任务分解（方案 A）

1. **判题镜像**（0.5 天）
   - `docker/Dockerfile.judge`：`python:3.11-slim`，无网络、无多余包，预置入口脚本
2. **`runner.py` 改造**（1.5~2 天）
   - 判题调用从 subprocess 改为：
     ```bash
     docker run --rm --network=none --memory=128m --cpus=0.5 \
       --pids-limit=64 --read-only -v <tmpdir>:/work:ro \
       -i interview-judge python /work/judge_entry.py
     ```
   - 保留现有四种判题类型（func / linked_list / tree / ops）与测试用例格式，`judge_entry.py` 即现有判题逻辑平移
   - 超时从 8s 改为「容器 wall-time 10s + 内容器 8s」双保险
   - 环境变量 `SANDBOX_MODE=docker|local`，本地开发仍可用 subprocess 模式（免装 Docker）
   - 容器池优化（可选，二期）：判题 QPS 低，先每次起容器（~300ms 开销可接受）
3. **降级与熔断**（0.5 天）
   - Docker daemon 不可用时：`/api/submit_code` 返回明确错误「判题服务暂不可用」，面试其他环节不受影响（**不能静默退回 subprocess**）
4. **安全测试**（0.5 天）
   - 用例：`import os; os.listdir('/')`、读 `/etc/passwd`、fork 炸弹、`while True`、申请 1GB 内存、尝试出网——确认全部被拦截且宿主无感知

**验收**：上述攻击用例全部失败且返回友好错误；正常 12 道题判题结果与改造前完全一致；单次判题端到端 < 2s。

---

## Phase 4：部署上线（2~3 天）

1. **Docker 化**（1 天）
   - `Dockerfile`：`python:3.11-slim` + `uv pip install` + uvicorn；`docker-compose.yml`：app 服务 + 挂载 `data/` 卷 + restart=always
   - 注意：判题容器需要宿主 `/var/run/docker.sock` 挂载进 app 容器（Docker-in-Docker 的 socket 方案），或判题与主服务拆成两个 compose 服务
2. **Nginx + HTTPS**（0.5 天）
   - 反代 `443 → 127.0.0.1:8000`，SSE 所需配置：`proxy_buffering off; proxy_read_timeout 300s; chunked_transfer_encoding on;`
   - 证书：云厂商免费 DV 证书或 certbot（Let's Encrypt），配自动续期
3. **前端 CDN 资源本地化**（0.5~1 天）
   - `index.html` 中 cdn.tailwindcss.com、jsdelivr（monaco/marked/dompurify）全部下载到 `static/vendor/`
   - Tailwind CDN 运行时版替换为预编译 CSS（`npx tailwindcss -o static/tailwind.css --minify`，或先直接本地化保留运行时版，二期再预编译）
4. **运维基线**（0.5 天）
   - uvicorn 日志切割；`/api/health` 接入云监控告警；`.env` 权限 600；服务器安全组只开 80/443/22

**验收**：外网 `https://域名` 全流程走通（注册→上传简历→完整面试→报告→历史→错题）；断网恢复会话正常；判题容器工作正常。

---

## 风险与注意

1. **判题 Docker socket 权限**：挂载 docker.sock 等于给 app 容器宿主 root 等价权限，二期应换 rootless docker 或独立判题机。一期用户量小可接受，但要在安全组层面限制服务器仅自用。
2. **匿名用户数据归属**：游客 → 注册账号后，是否合并 `ie_uid` 的历史？一期建议不合并（登录后只见账号数据），二期再做"认领游客数据"。
3. **SSE 与 Nginx**：必须关 buffering，否则流式输出变成整段延迟出现，务必实测。
4. **回归测试清单**：改造期间始终保持——12 道算法题判题、7 类八股、4 种模式、刷新恢复、200 条上限，手工过一遍。
