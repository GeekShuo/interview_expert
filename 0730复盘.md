# 0730 复盘 — AI 模拟面试系统 (interview_expert)

> 日期：2026-07-30
> 版本基线：v3.0（本地 commit `7c6a c76`，领先 origin 4+ 提交，push 受本机网络 TLS 阻断，需手动 push）

---

## 一、项目现状

- **技术栈**：FastAPI 后端 + 原生 JS 前端（无构建链），DeepSeek LLM（OpenAI 兼容 SDK）
- **完整闭环**：上传简历/JD → 打招呼 → 项目深挖 → 手撕代码 → 八股 → 评分报告 → 历史记录
- **运行**：`http://127.0.0.1:8000`（uvicorn），日志 `server.out.log` / `server.err.log`

## 二、本轮已完成（按提交）

| commit | 内容 |
|---|---|
| f2b69ad | v3 功能：进度栏 / 评分报告 / 历史记录 / 代码编辑器聚焦 / 话题护栏 |
| 8bbfaaf | **根因修复**：`_stream_llm` 检测 `###MEMO###` 后继续消费流拿全 `###CONTROL###`（此前 break 丢 next_stage → 进度栏卡死） |
| 965e1ec | 前端健壮：SSE JSON.parse 容错、流式出错复位、结束后禁发 |
| 25fb333 | 打磨：favicon 204、上传空文本提示 |
| 55feeb9 | 安全审查修复 S1/S2/S3/M1/M2/M3/M4/M5 |
| 7c6ac76 | S4 参考答案移出 prompt + B6 报告结论三选一，v3.0 标签移至此 |

### 关键修复明细
- **S1** `session.py` 并发锁（`threading.Lock` 包住 `stream_reply`）
- **S2** `main.py` 上传安全：5MB 上限、扩展名白名单、`%PDF` 魔数校验（413/415）
- **S3** `history.py` 原子写（tmp + `os.replace` + fsync + 写锁）
- **S4** coding 参考答案不进 prompt，只给「考察标签+评判原则」，防泄题
- **M1** 报告分数解析正则收紧（限前 250 字符 + 前缀要求）
- **M2** 报告防重入（`_report_done` 标志）
- **M3/M4** 前端 XSS：DOMPurify + `safeMd()` 全量替换 `marked.parse`、`escapeHtml` 补全
- **M5** LLM 客户端 `timeout=60s, max_retries=2`，错误文案友好化
- **PDF 解析失败**根因：运行环境缺 `pdfplumber`，已安装并用中文 PDF 验证

### 验证结论
- mock 全链路无回归；上传攻击面（exe/伪PDF/超大文件）全部正确拒绝
- QA 子 agent 结论「可发布」；代码审查评分从 6.5 → 可上线

### 遗留未修（有意搁置）
- **M6** 刷新丢进行中会话（→ 本计划 Phase 2 解决）
- **M7** 流式异常史少末句（已被锁+前端 finally 兜底，影响极小）

## 三、产品经理审核结论（上一轮）

> 核心判断：**功能闭环已成型，但「效果护城河」未建**——用户用一次觉得新鲜，但没有「非用不可、愿意付费」的理由。

按 ROI 排序：
- **P0（决定有没有用）**：① 代码环节真实运行+测试用例客观判题 ② 报告含可执行提升计划 ③ 简历解析结果可见可编辑
- **P1（决定留不留）**：④ 成长曲线与薄弱点画像 ⑤ 定向练习/难度选择 ⑥ 进行中面试可恢复
- **P2（打磨）**：⑦ 时间感 ⑧ 移动端适配 ⑨ 面试官风格 ⑩ 报告导出 ⑪ 语音 ⑫ 引导空状态
- **北极星指标**：4 周内「同用户 ≥3 场且平均分提升 ≥10 分」的用户占比

---

# 执行计划（0730 起）

> 原则：每个 Phase 完成 → 自测/QA → 提交 → 产品经理子 agent 审核 → 采纳意见进入下一 Phase。
> 目标：打磨到用户真正需要、愿意付费。

## Phase 1：核心效果 — 让评判「客观可信」（P0-1 + P0-2）
- [ ] 1.1 代码沙箱执行：后端 subprocess 隔离运行 Python 代码（超时/内存/禁网），跑题库测试用例
- [ ] 1.2 题库补测试用例：每题结构化 `tests: [{input, expected}]`，判题结果（通过 x/y）注入面试官评价
- [ ] 1.3 前端展示判题结果：用例通过情况、错误输出，面试官基于客观结果追问
- [ ] 1.4 报告升级：加入「可执行提升计划」——薄弱点 → 具体练习建议（题目/知识点清单）
- [ ] Phase 验收：跑通「写错代码 → 判题失败 → 面试官指出 → 修正 → 通过」全流程

## Phase 2：信任与留存 — 让用户「敢用、想回来」（P0-3 + P1-6 + P1-4）
- [ ] 2.1 简历解析结果预览/可编辑：上传后展示抽取要点，用户可修正再开始
- [ ] 2.2 会话持久化与恢复：刷新/关页后可继续进行中面试
- [ ] 2.3 成长曲线：历史页加分数趋势图、维度雷达图、薄弱点画像（跨场聚合）

## Phase 3：练习闭环 — 让用户「练得有方向」（P1-5）
- [ ] 3.1 定向练习模式：只练代码 / 只练八股 / 只练项目深挖
- [ ] 3.2 难度与方向选择：easy/medium/hard、方向标签（CV/NLP/推荐/LLM）
- [ ] 3.3 错题本：答错的八股/代码题自动入错题本，可重练

## Phase 4：前端体验大改版（对标市面优秀设计）
- [ ] 4.1 调研市面标杆（Final Round AI / HackerRank / interviewing.io / 面多多）视觉与交互
- [ ] 4.2 设计系统：统一配色/字体/间距/暗色模式，专业面试氛围
- [ ] 4.3 关键页重做：着陆页（价值主张+引导）、面试页（沉浸感+时间感）、报告页（可视化）
- [ ] 4.4 移动端适配 + 微交互（打字指示、阶段转场动效）

## Phase 5：商业化与打磨
- [ ] 5.1 报告导出（PDF/图片分享）
- [ ] 5.2 面试官风格开关（压力面/宽松面）
- [ ] 5.3 付费点设计：免费 N 场 → 深度报告/无限场次/定向练习收费（先做本地开关占位）
- [ ] 5.4 全链路 QA + 性能 + 安全回归

## 节奏
每完成一个 Phase：git commit → 子 agent（QA + 产品经理）审核 → 记录意见 → 修正 → 下一 Phase。
